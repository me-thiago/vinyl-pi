# Story V1.5-09: Gerar Documentação de API com Swagger/OpenAPI

**Epic:** V1.5 - Hardening & Quality (Post-Audit)

**Status:** done

**Prioridade:** 🟢 Médio

**Origem:** Relatório de Auditoria 2025-12-03 - Seção 10: Docs & DX (9/10)

**User Story:**
Como desenvolvedor,
quero ter documentação de API auto-gerada com Swagger UI,
para que possa explorar e testar endpoints facilmente.

## Contexto do Problema

A API não possui documentação interativa:

```
# Auditoria identificou:
- API docs não auto-gerados (sem Swagger/OpenAPI)
```

Isso dificulta exploração da API e integração com outras ferramentas.

## Critérios de Aceitação

1. [ ] swagger-jsdoc e swagger-ui-express instalados
2. [ ] Todos os endpoints documentados com JSDoc/OpenAPI
3. [ ] Swagger UI disponível em `/api/docs`
4. [ ] Schemas de request/response documentados
5. [ ] Autenticação documentada (se aplicável)
6. [ ] Exemplos de uso incluídos

## Pré-requisitos

- Nenhum

## Referências

- [Relatório de Auditoria](../../audit-report-2025-12-03.md) - Seção 10: Docs & DX
- [swagger-jsdoc](https://github.com/Surnet/swagger-jsdoc)
- [swagger-ui-express](https://github.com/scottie1984/swagger-ui-express)
- [OpenAPI Specification](https://swagger.io/specification/)

---

## Tasks/Subtasks

- [ ] Task 1: Instalar dependências
  - [ ] Adicionar swagger-jsdoc ao package.json
  - [ ] Adicionar swagger-ui-express ao package.json
  - [ ] Adicionar @types se necessário

- [ ] Task 2: Configurar Swagger
  - [ ] Criar `backend/src/config/swagger.ts`
  - [ ] Definir info, servers, tags
  - [ ] Configurar rota `/api/docs`

- [ ] Task 3: Documentar endpoints de Status
  - [ ] GET /api/status
  - [ ] GET /api/health

- [ ] Task 4: Documentar endpoints de Settings
  - [ ] GET /api/settings
  - [ ] PUT /api/settings
  - [ ] GET /api/settings/:key

- [ ] Task 5: Documentar endpoints de Sessions
  - [ ] GET /api/sessions
  - [ ] GET /api/sessions/:id
  - [ ] GET /api/sessions/:id/events

- [ ] Task 6: Documentar endpoints de Events
  - [ ] GET /api/events
  - [ ] GET /api/events/:id

- [ ] Task 7: Definir schemas de resposta
  - [ ] Schema Session
  - [ ] Schema AudioEvent
  - [ ] Schema Setting
  - [ ] Schema AudioStatus
  - [ ] Schema Error

---

## Implementação Sugerida

### Configuração Swagger

```typescript
// backend/src/config/swagger.ts
import swaggerJsdoc from 'swagger-jsdoc';
import swaggerUi from 'swagger-ui-express';
import { Express } from 'express';

const options: swaggerJsdoc.Options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Vinyl-OS API',
      version: '1.0.0',
      description: 'API para controle e monitoramento do sistema Vinyl-OS',
      contact: {
        name: 'Vinyl-OS',
        url: 'https://github.com/user/vinyl-os'
      }
    },
    servers: [
      {
        url: 'http://localhost:3001',
        description: 'Servidor de desenvolvimento'
      }
    ],
    tags: [
      { name: 'Status', description: 'Status do sistema e streaming' },
      { name: 'Settings', description: 'Configurações do sistema' },
      { name: 'Sessions', description: 'Sessões de escuta' },
      { name: 'Events', description: 'Eventos de áudio' }
    ]
  },
  apis: ['./src/routes/*.ts']
};

const specs = swaggerJsdoc(options);

export function setupSwagger(app: Express) {
  app.use('/api/docs', swaggerUi.serve, swaggerUi.setup(specs, {
    customCss: '.swagger-ui .topbar { display: none }',
    customSiteTitle: 'Vinyl-OS API Docs'
  }));
  
  // Endpoint para obter spec JSON
  app.get('/api/docs.json', (req, res) => {
    res.json(specs);
  });
}
```

### Exemplo de Documentação de Rota

```typescript
// backend/src/routes/status.ts

/**
 * @openapi
 * /api/status:
 *   get:
 *     tags:
 *       - Status
 *     summary: Obtém status atual do sistema
 *     description: Retorna informações sobre streaming, sessão ativa e dispositivos
 *     responses:
 *       200:
 *         description: Status do sistema
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/AudioStatus'
 *             example:
 *               isStreaming: true
 *               device: "plughw:1,0"
 *               sampleRate: 48000
 *               channels: 2
 *               listeners: 2
 *               session:
 *                 id: "abc-123"
 *                 startedAt: "2025-12-04T10:00:00Z"
 *                 eventCount: 15
 */
router.get('/', async (req, res) => {
  // ...
});

/**
 * @openapi
 * components:
 *   schemas:
 *     AudioStatus:
 *       type: object
 *       properties:
 *         isStreaming:
 *           type: boolean
 *           description: Se está transmitindo áudio
 *         device:
 *           type: string
 *           description: Dispositivo de áudio ALSA
 *         sampleRate:
 *           type: integer
 *           description: Taxa de amostragem em Hz
 *         channels:
 *           type: integer
 *           description: Número de canais (1=mono, 2=stereo)
 *         listeners:
 *           type: integer
 *           description: Número de ouvintes conectados
 *     Error:
 *       type: object
 *       properties:
 *         error:
 *           type: object
 *           properties:
 *             message:
 *               type: string
 *             code:
 *               type: string
 */
```

---

## Dev Agent Record

### Context Reference
- Story Context: [v1.5-09-swagger-openapi.context.xml](./v1.5-09-swagger-openapi.context.xml)

### Debug Log
- (a ser preenchido durante implementação)

### Completion Notes
- (a ser preenchido após conclusão)

---

## File List

### Arquivos a Criar
- backend/src/config/swagger.ts

### Arquivos a Modificar
- backend/package.json
- backend/src/index.ts
- backend/src/routes/status.ts
- backend/src/routes/settings.ts
- backend/src/routes/sessions.ts
- backend/src/routes/events.ts

---

## Change Log

- 2025-12-04: Story criada a partir do relatório de auditoria

