# Story V1.5-06: Configurar CI/CD com GitHub Actions

**Epic:** V1.5 - Hardening & Quality (Post-Audit)

**Status:** backlog

**Prioridade:** 🟡 Alto

**Origem:** Relatório de Auditoria 2025-12-03 - Seção 9: Testes & CI (7/10)

**User Story:**
Como desenvolvedor,
quero ter um pipeline de CI/CD configurado no GitHub Actions,
para que testes sejam executados automaticamente em cada push e pull request.

## Contexto do Problema

Atualmente não há CI/CD configurado:

```
# Auditoria identificou:
- Sem CI/CD configurado (GitHub Actions)
- Sem relatório de cobertura no CI
```

Isso significa que testes não são executados automaticamente, aumentando o risco de regressões.

## Critérios de Aceitação

1. [ ] Workflow GitHub Actions criado em `.github/workflows/ci.yml`
2. [ ] Testes do backend executados em push/PR para main
3. [ ] Testes do frontend executados em push/PR para main
4. [ ] Build verificado para ambos frontend e backend
5. [ ] Linting executado (ESLint)
6. [ ] Relatório de cobertura gerado (opcional: upload para Codecov)
7. [ ] Badge de status no README.md

## Pré-requisitos

- Testes já configurados (Jest/Vitest)

## Referências

- [Relatório de Auditoria](../../audit-report-2025-12-03.md) - Seção 9: Testes & CI
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

---

## Tasks/Subtasks

- [ ] Task 1: Criar workflow principal
  - [ ] Criar `.github/workflows/ci.yml`
  - [ ] Configurar trigger para push e PR em main
  - [ ] Configurar Node.js matrix (20.x)

- [ ] Task 2: Configurar jobs de backend
  - [ ] Job de lint (ESLint)
  - [ ] Job de testes (Jest)
  - [ ] Job de build (TypeScript)

- [ ] Task 3: Configurar jobs de frontend
  - [ ] Job de lint (ESLint)
  - [ ] Job de testes (Vitest)
  - [ ] Job de build (Vite)

- [ ] Task 4: Configurar cobertura (opcional)
  - [ ] Gerar relatório de cobertura
  - [ ] Upload para Codecov (se desejado)

- [ ] Task 5: Adicionar badge ao README
  - [ ] Badge de status do workflow
  - [ ] Badge de cobertura (se configurado)

---

## Implementação Sugerida

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Lint
        run: npm run lint
      
      - name: Run tests
        run: npm test -- --coverage
      
      - name: Build
        run: npm run build

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Lint
        run: npm run lint
      
      - name: Run tests
        run: npm test -- --coverage
      
      - name: Build
        run: npm run build
```

### Badge para README.md

```markdown
[![CI](https://github.com/USERNAME/vinyl-os/actions/workflows/ci.yml/badge.svg)](https://github.com/USERNAME/vinyl-os/actions/workflows/ci.yml)
```

---

## Notas

- O workflow assume que já existem scripts `lint`, `test`, e `build` em ambos package.json
- Se não existir script de lint, criar: `"lint": "eslint src --ext .ts,.tsx"`
- Considerar adicionar job de type-check separado: `"typecheck": "tsc --noEmit"`

---

## Dev Agent Record

### Context Reference
- Story Context: (a ser criado quando ready-for-dev)

### Debug Log
- (a ser preenchido durante implementação)

### Completion Notes
- (a ser preenchido após conclusão)

---

## File List

### Arquivos a Criar
- .github/workflows/ci.yml

### Arquivos a Modificar
- README.md (adicionar badge)
- backend/package.json (verificar scripts)
- frontend/package.json (verificar scripts)

---

## Change Log

- 2025-12-04: Story criada a partir do relatório de auditoria

