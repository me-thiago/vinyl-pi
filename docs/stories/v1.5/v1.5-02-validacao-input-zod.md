# Story V1.5-02: Adicionar Validação de Input com Zod

**Epic:** V1.5 - Hardening & Quality (Post-Audit)

**Status:** done

**Prioridade:** 🔴 Crítico

**Origem:** Relatório de Auditoria 2025-12-03 - Seção 2: Backend (8/10), Seção 7: Segurança (6/10)

**User Story:**
Como desenvolvedor,
quero que todas as rotas da API validem os dados de entrada usando Zod,
para que inputs malformados sejam rejeitados antes de processar.

## Contexto do Problema

Atualmente as rotas aceitam qualquer payload sem validação:

```typescript
// routes/settings.ts - aceita qualquer payload
app.post('/settings', (req, res) => {
  const settings = req.body; // ⚠️ Sem validação
});
```

Isso pode causar erros inesperados ou comportamento indesejado quando dados inválidos são enviados.

## Critérios de Aceitação

1. [x] Zod instalado como dependência do backend
2. [x] Schemas Zod criados para todos os endpoints que recebem body/params
3. [x] Middleware de validação genérico criado
4. [x] Rotas de settings validam payload com schema específico
5. [x] Erros de validação retornam 400 com mensagens claras em português
6. [x] Testes unitários para schemas de validação

## Pré-requisitos

- V1.5-01 (CORS) recomendado mas não obrigatório

## Referências

- [Relatório de Auditoria](../../audit-report-2025-12-03.md) - Seções 2 e 7
- [Documentação Zod](https://zod.dev/)

---

## Tasks/Subtasks

- [x] Task 1: Instalar e configurar Zod
  - [x] Adicionar zod ao package.json
  - [x] Criar `backend/src/schemas/index.ts` para exports centralizados

- [x] Task 2: Criar schemas de validação
  - [x] Schema para Settings (PATCH /api/settings)
  - [x] Schema para query params de Sessions (GET /api/sessions)
  - [x] Schema para query params de Events (GET /api/events)

- [x] Task 3: Criar middleware de validação
  - [x] Criar `backend/src/middleware/validate.ts`
  - [x] Suporte a validação de body, params e query
  - [x] Mensagens de erro em português BR

- [x] Task 4: Aplicar validação nas rotas
  - [x] Atualizar routes/settings.ts
  - [x] Atualizar routes/sessions.ts
  - [x] Atualizar routes/events.ts

- [x] Task 5: Testes
  - [x] Testes unitários para schemas
  - [x] Testes de integração para rotas

---

## Implementação Sugerida

```typescript
// backend/src/schemas/settings.schema.ts
import { z } from 'zod';

export const settingsSchema = z.object({
  audioDevice: z.string().optional(),
  silenceThreshold: z.number().min(-100).max(0).optional(),
  silenceDuration: z.number().min(1).max(300).optional(),
  sessionTimeout: z.number().min(1).max(1440).optional(),
  theme: z.enum(['light', 'dark', 'system']).optional(),
});

export type SettingsInput = z.infer<typeof settingsSchema>;

// backend/src/middleware/validate.ts
import { Request, Response, NextFunction } from 'express';
import { ZodSchema, ZodError } from 'zod';

export function validate(schema: ZodSchema, source: 'body' | 'query' | 'params' = 'body') {
  return (req: Request, res: Response, next: NextFunction) => {
    try {
      req[source] = schema.parse(req[source]);
      next();
    } catch (error) {
      if (error instanceof ZodError) {
        return res.status(400).json({
          error: {
            message: 'Dados de entrada inválidos',
            details: error.errors.map(e => ({
              campo: e.path.join('.'),
              mensagem: e.message
            }))
          }
        });
      }
      next(error);
    }
  };
}

// routes/settings.ts
import { validate } from '../middleware/validate';
import { settingsSchema } from '../schemas/settings.schema';

router.put('/', validate(settingsSchema), async (req, res) => {
  // req.body já está validado e tipado
});
```

---

## Dev Agent Record

### Context Reference
- Story Context: docs/stories/v1.5/v1.5-02-validacao-input-zod.context.xml

### Debug Log
- 2025-12-03: Implementação iniciada
- Instalado Zod v4.1.13 (versão mais recente)
- API do Zod v4 difere da v3: usa `issues` ao invés de `errors`, sintaxe de mensagens customizadas mudou
- Schemas criados com `.strict()` para rejeitar campos desconhecidos
- Middleware `validate()` suporta body, query e params
- Rotas atualizadas com validação via middleware (não inline)
- Testes existentes atualizados para novo formato de resposta de erro

### Completion Notes
- Implementação completa com 117 testes passando (55 novos + 62 atualizados)
- Zod v4 instalado e configurado
- Middleware genérico reutilizável criado
- Validação aplicada em: settings (PATCH), sessions (GET list e :id), events (GET)
- Erros retornam 400 com formato: `{ error: { message, code, details: [{campo, mensagem}] } }`
- Nota: Testes de audio-manager.test.ts com timeout não estão relacionados a esta story

---

## File List

### Arquivos Criados
- backend/src/schemas/index.ts
- backend/src/schemas/settings.schema.ts
- backend/src/schemas/sessions.schema.ts
- backend/src/schemas/events.schema.ts
- backend/src/schemas/pagination.schema.ts
- backend/src/middleware/validate.ts
- backend/src/__tests__/schemas/settings.schema.test.ts
- backend/src/__tests__/schemas/pagination.schema.test.ts
- backend/src/__tests__/middleware/validate.test.ts

### Arquivos Modificados
- backend/package.json (adicionado zod)
- backend/src/routes/settings.ts
- backend/src/routes/sessions.ts
- backend/src/routes/events.ts
- backend/src/__tests__/routes/sessions.test.ts (formato de erro)
- backend/src/__tests__/routes/events.test.ts (formato de erro)

---

## Change Log

- 2025-12-04: Story criada a partir do relatório de auditoria
- 2025-12-03: Implementação completa - Validação Zod com 117 testes passando

